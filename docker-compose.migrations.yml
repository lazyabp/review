version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=ReviewKit_Identity
      - ReviewKit_DB=ReviewKit_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
